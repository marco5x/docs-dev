---
export interface Props {
    name: string;
    link: string;
}

const { name, link } = Astro.props;
---

<iframe class="body" src={link || "0"} title={name}> </iframe>

<style>
    .body {
        width: 99.7%;
        height: 99vh;
    }
</style>
